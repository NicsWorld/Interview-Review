import { AppBar, Typography } from '@material-ui/core';

export default function Header() {
  return (
    <AppBar>
    <Typography variant="h6">
      Interview Review
    </Typography>
    </AppBar>
  );
}
